#include "omegaWTK/Widgets/Label.h"

namespace OmegaWTK {
    Label::Label(std::string text_to_draw,Widget * parent,bool isEnabled):Widget(parent,isEnabled){};
    void Label::render(){
        
    };
}